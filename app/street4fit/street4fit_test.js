'use strict';

describe('myApp.street4fit module', function() {

  beforeEach(module('myApp.street4fit'));

  describe('street4fit controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('Street4fitCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});